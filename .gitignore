# Dependencies
node_modules/

# Test output (generated coverage). Exception: the committed HW4 coverage
# HTML report lives under docs/coverage/ and is kept tracked.
coverage/
!/docs/coverage/

# Local env / secrets — never commit real Supabase keys.
.env
.env.local
.env.*.local
.env.scrape
src/config.js

# Editor / OS
.DS_Store
.vscode/
.idea/
*.log

# Claude Code harness state
.claude/
